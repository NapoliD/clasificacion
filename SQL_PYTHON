import teradata
import pandas as pd
 
udaExec = teradata.UdaExec(appName="Test", version="1.0",logConsole=False) 

session = udaExec.connect(method="odbc", system="DATA1N1",username="", password="")


sql=(
     "select  "
     "from xxx.xxx b left join xx.xx a on a.SESSION_ID = b.SESSION_ID "
     "where "
     "FEC_EVENT  between date '2019-07-12' and date '2019-07-24' "
                                
)



data_DW = pd.read_sql(sql,session) 
import os
os.chdir('C:')
